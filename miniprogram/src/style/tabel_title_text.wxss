/* font-weight,light(300), normal/regular(400), medium(500), bold(700) */


/* 对应微信：列表内标题 */
.box_title { 
  color: rgba(0, 0, 0, 0.9);font-size: 34rpx;font-weight:400;
}

/* 对应微信：看一看分组标题 */
.group_title {
  color: rgba(0, 0, 0, 0.9);font-size: 30rpx;font-weight:500;
}

/* 对应微信：表单预设内容、弹窗正文  */
.table_content {
  color: rgba(0, 0, 0, 0.5);font-size: 34rpx;font-weight:400;
  /* margin-top: 15rpx; */
}
.table_content_out {
  color: rgba(0, 0, 0, 0.8);
}

/* 对应微信：profile页描述 */
.description_content {
  color: rgba(0, 0, 0, 0.5);font-size: 28rpx;font-weight:400;
}



/* 表格格式 */
.table_head_color {
  background-color: #2c6e49;
  color: #fff;
}
 .table_content_color {
  color: #fff;
  background-color: #97a97c;
}
